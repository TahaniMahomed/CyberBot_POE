<Window x:Class="CyberBot_POE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CyberBot_POE"
        mc:Ignorable="d"
        Title="ShieldBot - Cybersecurity Assistant" Height="650" Width="450"
        Background="#121212" WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E1E" CornerRadius="8" Padding="10" Margin="0,0,0,15">
            <TextBlock Name="AsciiDisplay" 
                       FontFamily="Consolas" FontSize="8" 
                       Foreground="Cyan" HorizontalAlignment="Center" 
                       Text="[ShieldBot ASCII Loading...]"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <StackPanel Name="ChatContainer" VerticalAlignment="Bottom"/>
        </ScrollViewer>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Name="UserInputBox" Grid.Column="0" Height="40" 
                     VerticalContentAlignment="Center" Padding="10,0"
                     Background="#2D2D2D" Foreground="White" BorderThickness="1"
                     BorderBrush="Cyan" KeyDown="UserInputBox_KeyDown"/>

            <Button Name="SendButton" Grid.Column="1" Content="SEND" Width="70" Margin="10,0,0,0"
                    Background="Cyan" Foreground="Black" FontWeight="Bold"
                    Click="SendButton_Click"/>
        </Grid>
    </Grid>
</Window>